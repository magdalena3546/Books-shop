export const getAllBooks = ({books}) => books;

const BooksReducer = (statePart = [], action) => {
    switch (action.type) {
        default: 
            return statePart;
    }
}

export default BooksReducer;